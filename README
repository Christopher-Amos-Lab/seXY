1) Download seXY.R and XY_data.txt
2) XY_data.txt contains X heterozygosity and Y missingness values of the training set that is used to fit a logistic regression model
3) Enter the required inputs in seXY.R and run code in R
